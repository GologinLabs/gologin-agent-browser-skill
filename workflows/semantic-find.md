# Semantic Find

Use this workflow when raw refs are brittle or the page is highly dynamic.

1. Use `agent_browser_find` with `role`, `text`, `label`, or `placeholder`.
2. Pair it with actions like `click`, `fill`, `type`, or `text`.
3. Prefer this path for forms and dynamic UIs.
