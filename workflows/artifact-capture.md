# Artifact Capture

Use this workflow when the browser task needs files as outputs.

1. Use `agent_browser_screenshot` for visual state capture.
2. Use `agent_browser_pdf` for printable document artifacts.
3. Use `agent_browser_upload` when the page expects a file input.
